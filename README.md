# react-router-async-tryout

A sample app to try out the usage of React Router and
Async loading of data with React Redux.

Try the demo at https://will-moore.github.io/react-router-async-tryout/


To run:

	$ npm install
	$ npm start

	# Go to http://localhost:8080/
